"use strict";

var app = angular.module("appPrincipal", ['ngRoute']);

/*var app = angular.module('myModule', ['ngRoute']);

app.config(function($routeProvider) {
	$routeProvider
	.when("/drinkBeer", {
		controller: 'CtlDrinkIn',
		templateUrl : "static/beer.html"
	})
	.when("/presentation", {
		templateUrl : "vista/produccion.html"
	})
	.when("/production", {
		templateUrl : "vista/presentacion.html"
	})
	.when("/logo", {
		templateUrl : "vista/logo.html"
	})
	.otherwise({
		redirectTo: '/cerveza'
	});
});*/
